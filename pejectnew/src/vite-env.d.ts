/// <reference types="vite/client" />
/// <reference types="vite-plugin-svgr/client" />
// Added for SVGR plugin if we decide to use it for SVG components later.
// Can be removed if not used.

// You can also add other global type definitions here if necessary.
// For example, if you have global variables injected via HTML or other scripts:
// interface Window {
//   myGlobalVar?: string;
// }
